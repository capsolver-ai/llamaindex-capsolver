# Examples

`quickstart.py` exposes thin async functions to LlamaIndex `FunctionAgent`; every function delegates to `capsolver_agent.create_executor()`.

This repository does not publish a LlamaIndex adapter package. The example uses the shared CapSolver Agent library directly.
